import { SplashScreen } from "@/components/splash-screen"

export default function Loading() {
  return <SplashScreen size="md" text="Memuat Login" subtext="Mohon tunggu sebentar" showLogo={false} />
}
