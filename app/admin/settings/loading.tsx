import { SplashScreen } from "@/components/splash-screen"

export default function Loading() {
  return <SplashScreen size="md" text="Memuat Pengaturan" subtext="Mengambil data konten..." showLogo={false} />
}
